language: ruby
rvm:
  - 2.2.4p230
env:
  - RAILS_ENV=test
before_install:
  - gem install rack
services:
  - postgresql
before_script:
  - psql -c 'create database tepoztecatl_test;' -U postgres
script:
  - rails db:migrate
  - bundle exec rspec