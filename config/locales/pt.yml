# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t 'hello'
#
# In views, this is aliased to just `t`:
#
#     <%= t('hello') %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# To learn more, please read the Rails Internationalization guide
# available at http://guides.rubyonrails.org/i18n.html.

pt:
  activerecord:
    errors:
      messages:
        record_invalid: Houve um erro na sua requisição #{errors}
      models:
        storage:
          attributes:
            name: O nome não pode estar vazio
            code: O código não pode estar vazio
        measure_unit:
          attributes:
            name: O nome não pode estar vazio
            acronym: A sigla não pode estar vazia
        item:
          attributes:
            code: O código não pode estar vazio
            input_quantity: A quantidade de entrada não pode estar vazia
            output_quantity: A quantidade de saída não pode estar vazia
            input_measure_unit: É necessário informar a unidade de medida de entrada
            output_measure_unit: É necessário informar a unidade de medida de saída
